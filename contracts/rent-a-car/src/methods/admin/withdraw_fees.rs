use soroban_sdk::{Address, Env};

// use crate::storage::{car::read_car, types::{car_status::CarStatus, errors::Error}};

pub fn withdraw_fees(env: &Env, admin: &Address) -> Result<(), Error> {

    Ok(())
}
